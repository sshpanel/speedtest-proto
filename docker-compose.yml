version: "3.8"

services:
  app:
    build: .
    environment:
      - NODE_ENV=production
      - PORT=3000
    expose:
      - "3000"

  nginx:
    image: nginx:alpine
    depends_on:
      - app
    ports:
      - "7474:80"
    volumes:
      - ./nginx.conf:/etc/nginx/conf.d/default.conf:ro
